---

- name: Show input parameters
  debug:
    msg: "{{ item }}"
  loop:
    - "{{ INPUT_AWS_REGION }}"
    - "{{ INPUT_OS_CONFIG_TYPE }}"
    - "{{ INPUT_OS_TYPE }}"
    - "{{ INPUT_REQUIRED_EFS_TO_MOUNT }}"
    - "{{ INPUT_HOSTNAME }}"
    - "{{ INPUT_ENVIRONMENT }}"